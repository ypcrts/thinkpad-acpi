#include <stdlib.h>
#include <stdint.h> // because it's 2016
#include <string.h>
#include <unistd.h> // because stdio.h is slow
#include <stdio.h> // because we need sprintf

#define MIN_TIME 10
#define MED_TIME 100
#define MAX_TIME 300

//#define DEBUG
